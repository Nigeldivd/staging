export type Project = {
  url: string;
  title: string;
  description: string;
  source: string;
  alt: string;
};

export type Team = {
  source: string;
  identifier: string;
  position: string;
};

export type CountryList = {
  country: string;
};
